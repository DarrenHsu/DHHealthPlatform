extends layout

block content
    from(action="/loginProcess" method="post")
        p
            | 帳號：
            input(type="text", name="account", value="")
            | 密碼：
            input(type="text", name="password", value="")
        input(type='submit', value="登入")